# TASK-002 Execution Plan — Scaffold Expo App

## Objective

Create the initial `mobile/` Expo + React Native + TypeScript app using the current stable Expo Router starter, with a package lockfile and pinned Node major documented for repeatable local setup.

## In scope

- Scaffold `mobile/` with `create-expo-app`.
- Use the current stable Expo TypeScript starter with Expo Router.
- Preserve generated starter files without unrelated product customization.
- Add a Node major pin for the project.
- Run available starter verification commands.
- Update `PROJECT_STATE.md` and check off only `TASK-002` after verification.

## Out of scope

- Adding custom routes beyond the starter.
- Adding lint, test, or format scripts beyond generated defaults; that is TASK-003.
- Adding production dependencies such as Supabase, maps, forms, query libraries, or analytics.
- Creating EAS development-build configuration; that is TASK-005.
- Creating backend or Supabase files.

## Files expected to change

- `.nvmrc`
- `mobile/`
- `PROJECT_STATE.md`
- `TASKS.md`
- `docs/plans/TASK-002.md`

## Existing behavior inspected

- Read `AGENTS.md`, `PROJECT_STATE.md`, selected `TASK-002` in `TASKS.md`, `PRODUCT_SPEC.md`, `ARCHITECTURE.md`, and `docs/EXECPLAN_TEMPLATE.md`.
- Confirmed `mobile/` does not exist.
- Confirmed the workspace is still not a Git worktree, so commit/diff verification must be done through file inspection and command output.
- Checked current Expo documentation: SDK 57 is the latest stable SDK, `create-expo-app@latest --template default@sdk-57` is the documented TypeScript starter command during the SDK 57 transition, and SDK 57 requires Node 22.13.x minimum. The local Node version is v22.22.2.

## Implementation steps

1. Run `npx create-expo-app@latest mobile --template default@sdk-57 --yes`.
2. Add `.nvmrc` with the selected Node major `22`.
3. Inspect generated `mobile/package.json`, `app.json`, and route files for starter shape and Expo Router presence.
4. Run starter checks that are available without configuring TASK-003, at minimum `npm install` if needed and `npx expo-doctor`.
5. Attempt a local launch verification with `npx expo start --web` or another non-device command if supported by the generated starter; record truthfully if blocked by missing web dependencies or unavailable device/emulator.
6. Update `PROJECT_STATE.md`, check off `TASK-002` only, and fill completion evidence.

## Test plan

### Automated

- Command: `cd mobile; npx expo-doctor`
- Expected result: Expo project health checks pass, or any scaffold-related warnings are documented.

- Command: `cd mobile; npm run lint`
- Expected result: If generated by the starter, lint passes.

### Manual

- Device/environment: Local Windows machine without configured Android/iOS simulator.
- Steps: Inspect generated app structure and starter scripts; attempt to start the development server if practical without requiring a physical device.
- Expected result: Starter project exists, dependencies install from `package-lock.json`, and any launch limitation is documented rather than hidden.

## Risks and rollback

- Risk: The latest generator may choose a transitional SDK or template behavior that differs from older tutorials.
- Mitigation: Use the official SDK-specific template flag and record the SDK/Node choice.
- Rollback: Delete `.nvmrc` and `mobile/`, then revert documentation updates.

## Security/privacy review

- New data collected: None.
- Secrets involved: None.
- RLS/auth impact: None.
- Logging impact: None.

## Completion evidence

- Files changed: `.nvmrc`, `mobile/`, `PROJECT_STATE.md`, `TASKS.md`, `docs/plans/TASK-002.md`.
- Commands run and results: `npx create-expo-app@latest mobile --template default@sdk-57 --yes` succeeded; `npx expo-doctor` passed 20/20 checks; `npm run lint` failed once after auto-configuring ESLint, then passed after a minimal generated-hook fix; `npx tsc --noEmit` failed once on CSS import declarations, then passed after adding `mobile/src/types/css.d.ts`; `npm audit --audit-level=moderate` failed with 11 moderate advisories in Expo's generated dependency chain; `npm run web -- --non-interactive --port 8083` reached `Waiting on http://localhost:8083` before shutdown.
- Manual test result: Inspected generated `package.json`, `app.json`, `tsconfig.json`, and starter route files. Confirmed `package-lock.json` exists and removed the generated nested `mobile/.git`.
- Remaining limitations: Root folder is not a Git worktree, so no commit or normal `git diff` was possible. No iOS/Android device or emulator launch was performed. The starter remains generic Expo UI/content until later app-shell tasks.
- Acceptance criteria status: `mobile/` exists with the current stable Expo SDK 57 TypeScript starter and Expo Router; Node major is pinned to 22; lockfile exists; starter launches on web via Metro. Generated files are preserved except for minimal check-fix additions and removal of generated nested Git metadata.
